[Service]
# PAPER-only evidence cadence. The agent implementation clamps this to a
# minimum of 120 seconds and the separate hourly cap remains authoritative.
Environment="TRADING_AGENT_EXPLORATION_DROUGHT_S=120"
