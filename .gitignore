# secrets
firebase_key.json

# python
__pycache__/
*.pyc
# env
.env
# logs
*.log
# runtime report output (generated by audit bot and deploy loop)
reports/
Antigravity.exe
# Runtime state / server-local backups — never commit
server_local_backups/
data/paper_open_positions.json
# Local forensic/research/log extraction artifacts — never commit
data/research/
logs_extracted_tmp/
.claude/worktrees/
